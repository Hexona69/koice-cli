> For programatic use in Node.JS, see [koice.js](https://github.com/Hexona69/koice.js)

Download the binary and use it like:

```shell
$ > ./koice-linux-amd64 <BOT token> <channel id> -i /path/to/file
```
